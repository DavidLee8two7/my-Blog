type Blog {
  id: ID! @unique
  title: String!
  scientific: String
  effect: String!
  reference: String
  image: String
  largeImage: String
}

type Memo {
  id: ID! @unique
  name: String!
  email: String!
  subject: String!
  message: String!
}
