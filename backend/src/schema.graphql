# import * from './generated/prisma.graphql'

type Mutation {
  createSkill(
    title: String
    description: String
    image: String
    largeImage: String
  ): Skill!
}

type Query {
  skills: [Skill]!
}
