# import * from './generated/prisma.graphql'

type Mutation {
  createMemo(
    name: String!
    email: String!
    subject: String!
    message: String!
  ): Memo!
}

type Query {
  Memos: [Memo]!
}
